# Contract Team Reporting Tool — Feedback Tracker

Tool: `/contract-team-reporting`
Started: 2026-08-13

| # | Feedback | Status | Notes |
|---|----------|--------|-------|
| 1 | Counts are not correct on some of the views | Open | B&P Layout definitely wrong (item 4). Other views: user will re-verify counts AFTER the changes are made — keep this open until then |
| 2 | View description line should fully explain the view: which pipeline(s), any excluded pipeline stages, any filtered values | Done � verify | e.g. Full F&C shows only "View: Full F&C" — should also state the baked-in API exclusions (Finance/Settled and Construction/Handover excluded, open opportunities only) |
| 3 | View description must be AUTOMATED � derived from the actual view/filter config, not hardcoded text | Done � verify | Current implementation is partly hardcoded: e.g. Finance Tracking (Finance pipeline only) wrongly shows "Handover (Construction)" as an exclusion — irrelevant to that pipeline. Description must be computed from: view filters + API-level exclusions applicable to the pipelines actually in the view |
| 4 | B&P view must match the real B&P & Finance tool | Done � verify | Currently shows 690 records (all 4 pipelines) vs the actual B&P tool which filters to Finance pipeline / Type: Established / Stage ≠ Settled. Either remove the view or replicate the B&P tool's actual filters. Also: B&P quick-filter preset buttons (Blank P-type, No B&P Date, etc.) shown on ALL views — CONFIRMED: they should ONLY be shown when the B&P & Finance view is selected |
| 5 | Columns dropdown: group Opportunity / Custom Object fields, colour-coded | Done � verify | Also note from screenshot: "Assigned BA" appears twice in the list (opportunity assignedBA/assignedTo duplicate labels) — dedupe or distinguish labels when grouping |
| 6 | Same-named field collision (status) | Done � verify | Found ONE collision: `status`. (a) Label clash: Opportunity "Status" (open/won/lost) vs Custom Object "Status" field — grouping in item 5 must distinguish them. (b) DATA BUG: in the flat API record the property `status` value OVERWRITES the opportunity `status` — property fields need a prefix (e.g. `co_status`) or the merge must not clobber opportunity fields. Also confirmed "Assigned BA" dupe (item 5) comes from two different keys (`assignedTo` and `assignedBA`) both labelled "Assigned BA" in column definitions |
| 7 | Standard read-only columns across all views | Done � verify | |
| 8 | Use the CORRECT full GHL field names — no shortened names | Open | In the report copied to create the original Contract Team report, some GHL names were shortened. Not allowed in this report. Cross-check every field label against the CSV: `20260813 Property Record data and Associatred Opportunity Record Data info.csv` (has field names + IDs) and the GHL schema JSON (`module-1-output-bundles (18).json`) |
| 9 | Audit custom object fields pulled in vs the requested list | Done � verify (3-section column chooser + opp/co header tags built) | User believes fields were pulled in from the custom object that were NOT requested — which suggests some requested ones may also be MISSING. Reconcile the tool's field list against the CSV list (Column A = custom object fields requested, Column B = opportunity fields) both directions. DECIDED: keep the extra fields, but Columns dropdown gets a 3rd section "Other Custom Object fields" — do NOT mix them with the requested fields. All three sections sorted alphabetically. ALSO: columns placed into a report must indicate their source. DECIDED: small "opp" / "co" tag in the column header, colour-coded to MATCH the Columns dropdown section colours (opp = blue, co = light green). Tag can be tiny font — the colour does the work. Must NOT consume noticeable real estate (e.g. tiny coloured tag/dot in a corner of the header, no extra row/column) |
| 10 | Hide the Columns dropdown | Done � verify | DECIDED: reveal via Shift+double-click on the Settings (⚙) button — a different route from the hidden Edit Mode (Shift+double-click logo) |
| 11 | Ability to SAVE custom views | Done � see Phase 4 row below | DECIDED: when saving a view, user chooses PUBLIC (shared with everyone, stored server-side e.g. Redis) or PERSONAL (localStorage, this browser only). Named views with edit/delete. ALSO: drag-sort the views in the view menu, and organise the menu into named sections: "Standard reports", "Custom reports", "Exception reports" |
| 12 | View builder: choose which pipelines to include (or exclude) — tick boxes for pipelines AND pipeline stages | Open | Envisaged as checkbox selection of pipelines and stages per view |
| 13 | Assigned BA column showing owner instead of assignedBA field | Done � verify | The column is wired to `assignedTo` (owner). It must be wired to the `assignedBA` custom field (Property Team Info New folder, GHL field ID `NXqFwEzo28k6lOkbyT5N`). The real B&P tool shows the correct value; this tool's B&P Layout view does not |
| 14 | Colour choice per column (since Columns dropdown will be hidden, build colour config into it) | Open | Rules: must NOT override the grey used for non-editable (read-only) columns; column header must always be one shade darker than the column body |
| 15 | View button purple active state | Done � verify | Remove the purple active state from the View button |
| 16 | opp/co header tag: smaller, not bold, top-right of header cell | Done | User confirmed fine after refresh |
| 18 | Column colour coding (yellow/pink/blue groups, Formal Approval green etc.) must ONLY apply on the B&P view — remove from all other views | Open | Grey read-only colouring presumably stays global (confirm) |
| 17 | Rename "B&P & Finance" view to just "B&P" | Done — verify | |
| 18 | Column colour coding (yellow/pink/blue groups, Formal Approval green etc.) must ONLY apply on the B&P view — remove from all other views | Done — verify | Grey read-only colouring kept global (confirm that is correct) |
| 19 | B&P view count = 146 vs real B&P & Finance tool = 142 — identify the 4-record difference | ROOT CAUSE FOUND — decision needed | PRODUCTION (deployed commit 912f4e86) applies 2 exclusions in the B&P default view that the LOCAL code no longer does: (1) exclude records whose Registered Address contains "LOT"; (2) exclude 11 named opportunities listed in data/shay-report-exclusions.json. The local file src/app/api/shay-report/opportunities/route.ts has UNCOMMITTED changes removing both exclusions — that's why local shows 146 vs production 142. DECIDED: BOTH exclusions must be REMOVED ("the team need to clean data not exclude records"). Commit the uncommitted change removing them; production becomes 146 on next deploy. Reporting tool keeps matching the no-exclusions behaviour (already does) |
| 20 | Phase 4 decisions | Done — verify | (a) View sections: no Exception reports today but must be able to TAG views; Custom reports = Ocean Rise, Huntly, Golden Horizon; rest Standard. (b) Stage tick-boxes: pull live stage lists from GHL — approved. (c) Public view saving restricted to specific emails — build LATER. (d) Column colours: same palette as B&P tool is fine. PLUS end-state function per column: choose values (dropdown/tick) that mark the cell green — like the B&P tool's column settings |
| 11 | (Phase 4 build) Save views | Done — verify | View Builder panel (visible after Shift+dbl-click ⚙): save current columns/filters/sort as a named view, Personal (localStorage) or Public (Redis via /api/contract-team-reporting/views). View menu grouped Standard/Custom/Exception with drag-sort (⠿ handle) and delete (✕, builder mode only) |
| 12 | (Phase 4 build) Pipeline & stage tick-boxes | Done — verify | View Builder pulls live pipelines + stages from GHL (/api/contract-team-reporting/pipelines). Nothing ticked = all. Selections become view filters and flow into the automated view description |
| 21 | Full F&C edit test: many fields not saving / not writing back to GHL | Done � verify | Root cause: page saveRow whitelists only the 14 B&P fields and posts to /api/shay-report/update-opportunity which also only maps B&P field IDs. Fix: new /api/contract-team-reporting/update endpoint mapping ALL opportunity custom fields; page editable keys derived from the full field map (minus read-only) |
| 22 | Field types must match GHL: tick boxes where GHL has checkboxes; dropdowns where GHL has options | Done � verify | Registration's checkbox "on" value is "Registered" not "Yes" |
| 24 | Full audit of ALL opportunity fields vs GHL schema ("Opportunity fields.json" — includes picklistOptions for dropdowns) | Done — verify | Audited all 46 opportunity fields brought into the tool. FIXED: (a) Type of Property options were WRONG — GHL has [Established, Construction - Registered, Construction - Unregistered, SMSF Construction], tool had [Established, House & Land, Land Only, SMSF]; (b) Finance Extension Status was missing "Waived Finance" option; (c) Last Construction Update Date was not treated as a date field; (d) Settlement Date is TEXT in GHL, not DATE — removed from date handling; (e) Pre-settlement Inspection Status end-state config mentioned non-existent "Scheduled" option. NOTE: the production B&P & Finance tool ALSO has the wrong Type of Property options — needs the same fix (separate decision). assignedBA is SINGLE_OPTIONS in GHL but read-only in this tool so no dropdown needed |
| 25 | Custom object (co_) fields: audit types vs schema + wire tick boxes / dropdowns / dates + enable write-back | Done — verify | From the Property Reviews schema: 31 SINGLE_OPTIONS fields now render as dropdowns with exact GHL options (Deal Type, State, Title, Occupancy, Contract Type, Dwelling Type, beds/baths/garage counts, etc.); 1 CHECKBOX (push_record_to_deal_sheet); 1 DATE (review_date); 20 LARGE_TEXT get the expand editor. Write-back added: update endpoint now writes co_ fields to the linked Property Review record via the objects API (record id carried as co_record_id). NUMERICAL fields written as numbers. If a record has no linked property record, co_ edits are rejected with a clear error |
| 27 | Hourly live GHL schema check + auto email alert for new fields | Done — verify | New /api/contract-team-reporting/schema endpoint fetches opportunity + custom object schemas live from GHL, cached 1 hour; the page refreshes it hourly. Live schema takes priority for dropdown options / tick boxes / date types (new values appear within the hour, no deploy). Settings panel shows "GHL schema: refreshed HH:MM" + amber badge if unmapped new fields exist. Email alert (Gmail via existing ALERT_EMAIL_USER/PASSWORD) sent ONCE per newly created field to CTR_SCHEMA_ALERT_EMAILS (falls back to ALERT_EMAIL_TO) — baseline of existing fields stored in Redis so the ~100 out-of-scope legacy fields don't spam. Works identically in dev and production (same env vars) |
| 28 | Registration is not a strict tick box — how to handle unclear | ON HOLD — do nothing yet | In GHL the field IS a CHECKBOX with a single option "Registered" (so values are "Registered" or blank). But conceptually registration has more states (e.g. unregistered / ETA pending — there is a separate Registration Date ETA field). Options to discuss: (a) keep the tick box (matches GHL exactly); (b) ask GHL admin to convert the field to a dropdown with proper states, tool will pick it up via the hourly schema check; (c) render as a dropdown [blank, Registered] in the tool. No change until user decides |
| 29 | Make additional fields READ-ONLY in the tool (from GHL Opportunity details screenshots) | Open — DO NOT ACTION YET | Fields: Primary contact name, Primary email, Primary phone, Additional contacts, Opportunity name, Pipeline, Stage, Status, Value, Owner, Followers, Business name, Source, Expected Close Date, Tags, Partner Name, Partner Phone, Partner Email, Partner Address, Opportunity source. (Several already read-only in the tool: name, pipeline, stage, status, value/monetaryValue, owner, followers, contact fields — remaining to add when actioned: partnerName, partnerEmail, partnerPhone + any others brought in later) |
| 30 | Land Deposit Paid (and any field) may appear to revert after saving — race condition confirmed | Done — verify | CONFIRMED BUG: the 60-second background refresh replaced all rows wholesale; GHL's search API is eventually consistent, so a refresh landing shortly after a save could return stale values and visually revert the change (write actually succeeded in GHL). FIX: saved values are now remembered per record and overlaid on refreshed data for a 2-minute grace window, so stale refreshes can't revert them. Note: unsaved edits in edit mode were never at risk (kept in separate state) |
| 31 | Make ALL custom object (co_) fields read-only for the moment | Open — DO NOT ACTION YET | Write-back for co_ fields is built and working, but user wants them locked for now. When actioned: block co_ edits in the page + refuse co_ writes in the update API (easy to re-enable later) |
| 26 | Retire the old B&P & Finance tool once this tool is verified | Open | User indicated the production /bp-finance report will be retired — its wrong Type of Property options won't need fixing if retired. Decide timing |
| 23 | Contact Email + Contact Phone must be read-only | Done � verify | They're contact-record fields, not opportunity custom fields — cannot be written via the opportunity API anyway |
| 14 | (Phase 4 build) Column colours + end states | Done — verify | Per-column colour (B&P palette) + end-state values (tick from dropdown options or observed values → cell goes green) editable in the View Builder, saved with the view. Read-only grey always wins; header shade darker than body (palette pairs) |
